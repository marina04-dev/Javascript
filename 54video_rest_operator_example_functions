function fun(initial,...rest) {
    let result=initial;
    for (let item of rest) {
        result *= item;
    }
    return result;
}
console.log(fun(1,2,3,4,5))


/* default */
function myf(a,b="default",c) {
    console.log(a,b,c);
}
myf(1,2,3)
